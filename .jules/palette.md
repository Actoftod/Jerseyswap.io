## 2024-05-22 - Missing ARIA Labels on Icon Buttons
**Learning:** Icon-only buttons (like navigation icons) are invisible to screen readers without `aria-label`. This is a common pattern in the app's header and floating action buttons.
**Action:** Always verify icon-only buttons have descriptive `aria-label` attributes during implementation.
